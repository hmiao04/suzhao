<div style="width: 600px;">
    <p><b>发起用户</b>:{{ tsk.member_name }}</p>
    <p><b>发起时间</b>:{{ tsk.created_date }}</p>
    <p><b>酬金(元)</b>:{{ tsk.paid_price }}</p>
    <b>主图:</b>
    <p><img src="{{ res_url }}{{ tsk.main_image }}" style="max-height: 250px"></p>
    <b>首页:</b>
    <div>
        <p class="change-image" data-upload-success="task.update?id={{ tsk.id }}" data-upload-name="home_image">
            <img src="{{ res_url }}{{ tsk.home_image }}" style="max-height: 250px;min-height: 100px;min-width: 200px;border: solid 1px #eee;">
        </p>
    </div>
    <b>支付状态:</b>
    <p>
        <select class="form-control change-ajax-post" name="pay_status" data-value="{{ tsk.pay_status }}"
                data-post-url="task.update?id={{ tsk.id }}" data-status-ele="#pay_status_{{ tsk.id }}">
            <option value="1" {{ tsk.pay_status == 1?'selected="selected"' }}>已支付</option>
            <option value="0" {{ tsk.pay_status == 0?'selected="selected"' }}>未支付</option>
        </select>
    </p>
    <b>状态:</b>
    <p>
        <select class="form-control change-ajax-post" name="status" data-value="{{ tsk.status }}"
                data-post-url="task.update?id={{ tsk.id }}" data-status-ele="#status_{{ tsk.id }}">
            <option value="1" {{ tsk.status == 1?'selected="selected"' }}>未接</option>
            <option value="2" {{ tsk.status == 2?'selected="selected"' }}>已接</option>
            <option value="3" {{ tsk.status == 3?'selected="selected"' }}>已取消</option>
        </select>
    </p>
</div>